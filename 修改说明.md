# 锅炉监控系统 v2 修改说明

## 一、接口与端口对齐

| 问题 | 修复 |
|---|---|
| `config.json` 的 `interval: 5` 从未被读取，模拟器硬编码 2 秒 | 两个进程统一读取同一个 `config.json`（周期、端口、阈值、PID 参数） |
| ModBus 换算自相矛盾（温度÷10、压力×10，注释与实际相反） | 统一寄存器映射表：0=温度×10，1=水位×10，2=压力(kPa)；阀门 10-12 号寄存器×100 |
| `write_modbus_valves()` 是死代码，阀门指令从不回写 | 每个控制周期自动回写三阀门；新增 `POST /api/modbus-collect` 全回路接口 |
| API 算完阀门就丢弃，系统实为开环 | API 响应带回三阀门指令，模拟器执行——**闭环** |
| compose 没有 api 服务，README 却称"完整环境" | compose 新增 `api` 服务 + 补上缺失的 `Dockerfile` 和 `requirements.txt` |
| 数据库配置写死在代码里 | 统一走 `config.json`，容器内用 `DB_HOST` 等环境变量覆盖 |
| README 两个"快速开始"、代码块未闭合、git 地址损坏 | 全部重写 |

## 二、逻辑错误修复

1. **水位报警方向错误（最严重）**：原逻辑"水位 > 10% 就报警"，正常水位 50% 意味着**系统永远在报警**，而锅炉真正的杀手——缺水——毫无防护。改为四阈值：高水位 ≥90%、缺水 ≤25%、超压 ≥1.5 MPa、超温 ≥160°C。
2. **压力阈值与控制目标打架**：报警线 0.1 MPa 低于 PID 目标 1.0 MPa，到达控制目标必然报警。报警线提至 1.5 MPa。
3. **压力单位链混乱**：模拟器按 ×10⁵ 输出、API 按 ÷10⁵ 换算、却都标注为 Pa（1 MPa = 10⁶ Pa）。全链统一为 SI 单位 Pa。
4. **压力 PID 接错阀门（正反馈）**：压力低 → 开大蒸汽阀 → 压力更低。改由**进料阀**控制压力（加热升压），这也是接入进料阀的天然位置。
5. **物理模型发散**：原模型热量只进不出，温度几分钟顶到 200°C 上限。重新配平产汽/耗汽/热惯性，闭环验证稳态：水位 50.9%、压力 1.018 MPa、温度 113.7°C，500 步零报警。
6. **PID 无抗饱和**：加入条件积分抗饱和 + 稳态前馈偏置（消除冷启动迟滞）。

## 三、三阀门新系统

- **进料阀 feed_valve**：压力 PID 自动控制
- **进水阀 water_valve**：水位 PID 自动控制
- **蒸汽阀 steam_valve**：自动模式下由负荷侧（现场）决定；手动模式或超压联锁时由系统下发

**安全联锁**（优先级高于 PID 和手动模式）：

| 工况 | 进料阀 | 进水阀 | 蒸汽阀 |
|---|---|---|---|
| 缺水 ≤25% | 0（停炉） | 100（强制补水） | 0 |
| 超压 ≥1.5MPa | 0 | 保持 | 100（紧急泄压） |
| 超温 ≥160°C | 0 | 保持 | — |
| 高水位 ≥90% | 保持 | 0 | — |

**新增接口**：`GET/POST /api/valves`（auto/manual 模式切换、手动给定三阀开度），
联锁在手动模式下依然生效。数据库新增 `feed_valve` 列，老库自动迁移不丢数据。

## 四、验证结果

- API 逻辑 10 项测试全部通过（正常/缺水/超压/超温/高水位/手动/联锁覆盖手动/非法参数）
- 端到端闭环 600 步：稳态精确跟踪设定值，负荷扰动下零误报
- 故障注入：进料阀卡死→7 步超温报警；进水故障→15 步缺水报警；进水卡开→16 步高水位报警

## 启动方式

```bash
docker compose up --build      # postgres + api + grafana
python sensor_simulator.py     # 宿主机跑闭环仿真
```

## 五、管理脚本（v2.1 补充）

旧 `manage.ps1` 无法使用的原因：psql 指向不存在的 `admin/boiler_db`；
`up` 提示语还停留在"API 宿主机启动"的旧流程；test 数据在新规则下必触发缺水报警。

现为双平台脚本，命令一一对应：

| 命令 | 作用 |
|---|---|
| `up` | 一键启动 postgres + api + grafana（Docker） |
| `dev` | 仅起数据库 + Grafana，API 留宿主机调试 |
| `api` | 宿主机跑 Flask（配合 dev 使用） |
| `test` | 发送正常工况数据，验证闭环响应 |
| `sim` / `db` / `logs` / `down` / `status` | 仿真器 / 进数据库 / 日志 / 停止 / 状态 |

Windows：`.\manage.ps1 up`；Linux/macOS：`bash manage.sh up`。
